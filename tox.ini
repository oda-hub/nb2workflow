[tox]
envlist = py39

[testenv]
extras = test, service, rdf
deps = pytest
commands = pytest -k 'not cwl and not docker and not deploy' {posargs}

[testenv:py39-cwl]
extras = test, service, rdf, cwl
deps = pytest
commands = pytest -k 'cwl' {posargs}

[testenv:py39-docker]
extras = test, service, rdf, docker
deps = pytest
commands = pytest -k 'docker' {posargs}
