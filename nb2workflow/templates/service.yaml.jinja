apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ deployment_name }}
  name: {{ deployment_name }}
  namespace: {{ namespace }}
spec:
  selector:
    app: {{ deployment_name }}
  type: ClusterIP
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
